const nextConfig = {
    images: {
      remotePatterns: [
        {
          protocol: "https",
          hostname: "**",
        },
      ],
        domains: ['d2prwyp3rwi40.cloudfront.net'],
      },
}

export default nextConfig;
